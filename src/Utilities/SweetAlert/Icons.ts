import Swal,{ SweetAlertIcon, SweetAlertResult } from "sweetalert2";

const infoIcon: SweetAlertIcon = "info";
const warningIcon: SweetAlertIcon = "warning";
const questionIcon: SweetAlertIcon = "question";
const errorIcon: SweetAlertIcon = "error";

let result: SweetAlertResult;

export { infoIcon, questionIcon, warningIcon, result, errorIcon, Swal };
